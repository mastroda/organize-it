import { StatoTodoPipe } from './stato-todo.pipe';

describe('StatoTodoPipe', () => {
  it('create an instance', () => {
    const pipe = new StatoTodoPipe();
    expect(pipe).toBeTruthy();
  });
});
